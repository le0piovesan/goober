import { Heading } from "@chakra-ui/react";
import { type NextPage } from "next";
import Layout from "~/components/Layout";

const Call: NextPage = () => {
  return (
    <Layout>
      <Heading>Call a Goober Driver</Heading>
    </Layout>
  );
};

export default Call;
